# War for the Overworld - 独立版本依赖 v1.1.0
# 运行独立Python版本所需的库

# 核心游戏引擎
pygame>=2.0.0
emoji>=2.0.0

# 可选依赖 (用于高级功能)
# numpy>=1.21.0        # 数学计算优化 (可选)
# pillow>=8.0.0        # 图像处理 (可选)

# 开发依赖 (仅开发时需要)
# pytest>=6.0.0       # 单元测试 (开发用)
# black>=21.0.0        # 代码格式化 (开发用)
